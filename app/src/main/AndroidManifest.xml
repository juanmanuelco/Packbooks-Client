<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.juanm.packbooks">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.INTERNAL_SYSTEM_WINDOW"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".principal" />
        <activity
            android:name=".Subir"
            android:parentActivityName=".principal" />
        <activity
            android:name=".Perfil"
            android:parentActivityName=".principal" />
        <activity
            android:name=".Registrarse"
            android:parentActivityName=".Login" />
        <activity
            android:name=".Acerca"
            android:parentActivityName=".principal" />
        <activity
            android:name=".Confirmar"
            android:parentActivityName=".Login" />
        <activity
            android:name=".Recuperar"
            android:parentActivityName=".Login" />
        <activity
            android:name=".Olvido_contra"
            android:parentActivityName=".Login" />
        <activity android:name=".bajarLibros" android:parentActivityName=".principal" />
        <activity android:name=".eliminarLibros" android:parentActivityName=".principal"></activity>
    </application>

</manifest>